machine:
  kernel:
    modules:
      - name: drbd
        parameters:
          - usermode_helper=disabled
      - name: drbd_transport_tcp
  nodeLabels:
    storage: miroir
    storage.miroir.home-operations.com/class: std
